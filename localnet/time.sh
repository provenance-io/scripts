#!/bin/bash

# 1 Approach is we hijack system calls
We would need to build the library once on the docker container
We would need to LD_PRELOAD
We need to be able to restart the CMD on docker so that it can use LD_PRELOAD

# 2 We setup virtualbox


# Set to current
# Advance Time
# More advanced commands around advance time